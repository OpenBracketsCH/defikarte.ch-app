import React from 'react';
import { View, Text, StyleSheet } from 'react-native';

const AboutScreen = ({ navigation }) => {
  return (
    <View>
      <Text>Defikarte.ch</Text>
    </View>
  );
}

const styles = StyleSheet.create({});

export default AboutScreen;